PROJECT_NAME="apps-4-us"

# User IDs for proper file permissions in Docker
# Run 'id -u' to get your UID and 'id -g' to get your GID
CONTAINER_UID=1000
CONTAINER_GID=1000

# Timezone Settings
TIMEZONE=Europe/Berlin

# Email Verification Settings
EMAIL_VERIFICATION_REQUIRED=true

DOCKER_NETWORK_NAME="${PROJECT_NAME}-network"

FE_URL="app.${PROJECT_NAME}.localhost"
BE_URL="api.${PROJECT_NAME}.localhost"
PHP_MY_ADMIN_URL="phpmyadmin.${PROJECT_NAME}.localhost"
REDIS_URL="redis.${PROJECT_NAME}.localhost"
REDIS_INSIGHT_URL="redisinsight.${PROJECT_NAME}.localhost"
MAIL_URL="mail.${PROJECT_NAME}.localhost"
MINIO_URL="minio.${PROJECT_NAME}.localhost"
REVERB_URL="reverb.${PROJECT_NAME}.localhost"

MYSQL_URL="mysql.${PROJECT_NAME}.localhost"
MYSQL_DB="${PROJECT_NAME}"
MYSQL_USER="${PROJECT_NAME}"
MYSQL_PASSWORD="${PROJECT_NAME}"
MYSQL_ROOT_PASSWORD="${PROJECT_NAME}"

# Laravel Application Settings
APP_ENV=local
APP_DEBUG=true
APP_KEY=base64:tm0cwBsRuQ/23W8mwGgTiLbsBmU67mNMjGacBoaPs6A=

# Locale Settings
APP_LOCALE=de
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=de_DE

# Cache & Session
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# Redis Settings
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Session Configuration
SESSION_LIFETIME=120
SESSION_DOMAIN=".${PROJECT_NAME}.localhost"
SANCTUM_STATEFUL_DOMAINS="app.${PROJECT_NAME}.localhost,localhost:3000"

# Horizon Configuration
HORIZON_PREFIX="horizon:${PROJECT_NAME}"
HORIZON_MEMORY_LIMIT=64

# Mail Configuration (Inbucket for local dev)
MAIL_MAILER=smtp
MAIL_HOST=inbucket
MAIL_PORT=2500
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@${PROJECT_NAME}.localhost"
MAIL_FROM_NAME="${PROJECT_NAME}"

# Laravel Filesystem Configuration (S3-compatible MinIO)
FILESYSTEM_DISK=s3
AWS_ACCESS_KEY_ID="${PROJECT_NAME}"
AWS_SECRET_ACCESS_KEY="${PROJECT_NAME}"
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET="${PROJECT_NAME}"
AWS_ENDPOINT=http://minio:9000
AWS_USE_PATH_STYLE_ENDPOINT=true

# MinIO Configuration
MINIO_ROOT_USER="${PROJECT_NAME}"
MINIO_ROOT_PASSWORD="${PROJECT_NAME}"
MINIO_BUCKET="${PROJECT_NAME}"
MINIO_REGION=us-east-1

# Broadcasting (Laravel Reverb)
BROADCAST_CONNECTION=reverb
REVERB_APP_ID="${PROJECT_NAME}"
REVERB_APP_KEY=reverb-app-key
REVERB_APP_SECRET=reverb-app-secret
REVERB_HOST="reverb.${PROJECT_NAME}.localhost"
REVERB_PORT=443
REVERB_SCHEME=https
VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT=443
VITE_REVERB_SCHEME=https

# Webhook Configuration
WEBHOOK_RATE_LIMIT_DEFAULT=10
WEBHOOK_MAX_REQUEST_SIZE_KB=128
MAX_RESPONSES_PER_WEBHOOK_ROUTE=5

# Project Limits
MAX_PROJECTS_PER_TYPE=1
MAX_WEBHOOKS_PER_PROJECT=5
